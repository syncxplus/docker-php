```
docker run --name ${appName} -v ${appPath}:/var/www/html -p 80:80 -d ${image}:${tag}
```

```
gm convert origin.jpg -font SourceHanSansSC-Normal -fill red -pointsize 50 -draw 'text 100,100 "思源黑体"' result.png
```
